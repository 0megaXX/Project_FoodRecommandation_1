<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <data>
        <import type="android.graphics.drawable.Drawable"/>
        <variable
            name="foodTag"
            type="String" />
        <variable
            name="foodCategoryName"
            type="String" />
        <variable
            name="foodImage"
            type="Drawable" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <CheckBox
            android:id="@+id/cb_food_category"
            android:layout_width="20dp"
            android:layout_height="20dp"
            app:buttonTint="@color/primary"
            app:layout_constraintBottom_toTopOf="@+id/backgroundView"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <View
            android:id="@+id/backgroundView"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:background="@drawable/background_circle"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/cb_food_category"
            app:layout_constraintTop_toBottomOf="@+id/cb_food_category" />

        <ImageView
            android:id="@+id/ib_food"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@{foodImage == null ? @drawable/south_korea : foodImage}"
            android:stateListAnimator="@animator/button_elevation"
            android:tag="@{foodTag}"
            app:layout_constraintTop_toTopOf="@+id/backgroundView"
            app:layout_constraintBottom_toBottomOf="@+id/backgroundView"
            app:layout_constraintEnd_toEndOf="@+id/backgroundView"
            app:layout_constraintStart_toStartOf="@+id/backgroundView"
            tools:src="@drawable/south_korea"/>

        <TextView
            android:id="@+id/tv_food_category"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="5dp"
            android:layout_marginBottom="5dp"
            android:gravity="center"
            android:text="@{foodCategoryName == null ? `카테고리` : foodCategoryName}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="@+id/backgroundView"
            app:layout_constraintStart_toStartOf="@+id/backgroundView"
            app:layout_constraintTop_toBottomOf="@+id/backgroundView"
            tools:text="카테고리"
            android:textColor="@color/gray_500"
            android:textAppearance="@style/text_m12"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>